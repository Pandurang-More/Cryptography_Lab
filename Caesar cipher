Explanation of the Code :
The program implements the Caesar Cipher, a simple encryption technique where each
letter in the text is shifted by a fixed number of positions in the alphabet.

1. Encryption Function (encryption)
o Takes a key k and a string s.
o Shifts each alphabetic character by k positions.
o Works separately for uppercase (A–Z) and lowercase (a–z).

2.Decryption Function (Decryption)
o Reverses the process by shifting each character backward by k positions.
o Ensures correct results by adding +26 before applying modulo %26 to handle negative shifts.

3. Main Function
o Asks the user whether they want to Encrypt or Decrypt.
o Takes the input string and key value.
o Calls the appropriate function and prints the result.




#include<iostream>
using namespace std;



 string encryption (int k,string s)
    {
    for(int i=0;i<s.length();i++)
    {
        if(s[i]>='a' && s[i]<='z') 
        {
            s[i]=(s[i]-'a'+k)%26+'a';
        }
        else if(s[i]>='A' && s[i]<='Z')
        {
            s[i]=(s[i]-'A'+k)%26+'A';
        }
    }
    return s;
  }     
  


   string  Decryption (int k,string s)
    {
        for(int i=0;i<s.length();i++)
        {
            if(s[i]>='a' && s[i]<='z') 
            {
                s[i]=(s[i]-'a'-k+26)%26+'a';
            }
            else if(s[i]>='A' && s[i]<='Z')
            {
                s[i]=(s[i]-'A'-k+26)%26+'A';
            }
        }
        
        return s;
    }
int main()
{    

     cout<<"**********Caesar Cipher**********\n";
     cout<<"what do you want to do? \n Encryption(1) or Decryption(2)\n";
    int choice;
    cin>>choice;
    string s;
    int k;
    if(choice==1)
    {
        cout<<"Enter the string for encryption: \n";
        cin.ignore();
        getline(cin,s);
        cout<<"Enter the key for encryption:\n";
        cin>>k;
        cout<<"Encrypted string is: \n";
        cout<< encryption(k,s)<<endl;
    }
    else if(choice==2)
    {
        cout<<"Enter the string for Decryption: \n";
        cin.ignore();
        getline(cin,s);
        cout<<"Enter the key for Decryption:\n";
        cin>>k;
        cout<<"Decrypted string is:\n ";
        cout<<Decryption(k,s)<<endl;
    }
    else
    {
        cout<<"Invalid Input\n";
    }

    

    return 0;
}
